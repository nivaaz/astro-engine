/**
 * @astro-engine/schema
 * 
 * Shared TypeScript types, validators, and constants.
 * ZERO runtime dependencies — pure types and enums.
 * 
 * See: ../../specs/astrology-event.schema.ts for canonical types
 */

export const SCHEMA_VERSION = "0.1.0";

// TODO: Move canonical types from specs/ into this package
// export type { AstrologyEvent, Planet, ZodiacSign, ... } from './types';
