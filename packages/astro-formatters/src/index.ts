/**
 * @astro-engine/formatters
 * 
 * Format astrology data into multiple output formats:
 * JSON · Markdown · ICS · Plain Text · HTML · CSV
 * 
 * Formatters NEVER modify event data — they only render it.
 */

export const FORMATTERS_VERSION = "0.1.0";
