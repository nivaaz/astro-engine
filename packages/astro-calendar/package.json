{
  "name": "@astro-engine/astro-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "ICS feed generation and calendar subscription management",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@astro-engine/astro-formatters": "*",
    "@astro-engine/schema": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}